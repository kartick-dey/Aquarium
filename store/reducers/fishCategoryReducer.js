import * as Data from '../../data/dumyData';

const initialState = {
    fishCategory: Data.fishCategory
};

const fishCategoryReducer = (state = initialState, action) => {
    return state;
}

export default fishCategoryReducer;